Run `docker build -t hello-docker .` to build docker image for this project.

Run `docker images` to view all the docker images on your computer.

Run `docker run hello-docker` to run docker image of this project.
